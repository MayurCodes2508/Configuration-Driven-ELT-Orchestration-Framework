{

        "type": "object",

        "required": ["storage_type", "bucket", "layer", "format", "path_template"],
    

        "properties": {

            "storage_type": {

              "type": "string",
              "const": "GCS"
            },

            "bucket": {

                "type": "string",
                "minLength": 2,
                "maxLength": 100
            },

            "layer": {

                "type": "string",
                "enum": ["dev", "staging", "prod"]
            },

            "format": {

                "type": "string",
                "minLength": 2,
                "maxLength": 50
            },

            "path_template": {
                
                "type": "string",
                "const": "{layer}/{source}/{dataset}/{entity}/ingestionDT={ingestionDT}/ingestionTS={ingestionTS}/jobRunID={jobRunID}.{format}"
            }
        },
        "additionalProperties": false
    }
